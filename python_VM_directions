to start the python vm, first download the Random_Site file. 
navigate to the Random_Site directory and use the run server command to start the server. 
when the server starts it should show an address where the app can be accessed
